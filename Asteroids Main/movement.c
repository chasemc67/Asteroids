//draw ship (and asteroids)
    //check global transform and change sx and sy accordingly

//if ship.speed > 0;
    //move ship bt ship.speed;
    //ship.speed -= 1;

    //not sure if transformation should be changed here or in controls.c